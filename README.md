# superscraper
SuperScraper is an n8n workflow that operates as a Telegram bot to scrape web content. Users can send a single URL, a text file containing multiple URLs, or a sitemap file. The bot fetches the HTML, converts it into clean Markdown, and returns the results directly in the chat. It includes built-in rate limiting, progress tracking, and more.
